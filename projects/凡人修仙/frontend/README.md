# 凡人修仙 - 前端项目

修仙主题的个人修炼记录应用前端界面。

## 技术栈

- **React 18** + **TypeScript** - 前端框架
- **Vite** - 构建工具
- **TailwindCSS** - CSS框架
- **React Router** - 路由管理
- **Axios** - HTTP客户端
- **Lucide React** - 图标库

## 功能特性

### 已实现页面

1. **首页/Landing** - 应用介绍，修炼入口
2. **登录/注册** - 用户认证（道号注册）
3. **八字测算** - 输入生日，测算灵根类型
4. **修炼大厅** - 开始/结束修炼，查看天时信息
5. **AI道友** - 与修仙AI聊天对话
6. **每日箴言** - 今日箴言+感悟记录（含《渔樵问对》）
7. **修炼记录** - 历史修炼数据统计
8. **个人中心** - 用户信息/灵根/经验等级

### 设计特点

- 🎨 **修仙主题风格** - 古风/仙气UI设计
- 📱 **响应式布局** - 支持移动端
- 🌙 **深色模式优先** - 沉浸式修炼氛围
- ✨ **加载动画/过渡效果** - 流畅的交互体验

## 项目结构

```
src/
├── components/       # 公共组件
│   └── Navigation.tsx
├── contexts/         # React Context
│   └── AuthContext.tsx
├── pages/            # 页面组件
│   ├── HomePage.tsx
│   ├── LoginPage.tsx
│   ├── RegisterPage.tsx
│   ├── CultivatePage.tsx
│   ├── BaziPage.tsx
│   ├── ChatPage.tsx
│   ├── ProverbPage.tsx
│   ├── HistoryPage.tsx
│   └── ProfilePage.tsx
├── services/         # API服务
│   └── api.ts
├── types/            # TypeScript类型
│   └── index.ts
├── App.tsx           # 主应用组件
├── main.tsx          # 入口文件
└── index.css         # 全局样式
```

## 快速开始

### 安装依赖

```bash
cd ~/.openclaw/workspace/projects/凡人修仙/frontend
npm install
```

### 配置环境变量

复制 `.env.example` 为 `.env.local` 并修改：

```bash
cp .env.example .env.local
```

编辑 `.env.local`：
```
VITE_API_URL=http://localhost:3001/api/v1
```

### 启动开发服务器

```bash
npm run dev
```

应用将运行在 http://localhost:5173

### 构建生产版本

```bash
npm run build
```

构建结果将在 `dist/` 目录中。

## API对接

后端API地址：`http://localhost:3001`

### 认证相关
- `POST /api/v1/auth/register` - 注册
- `POST /api/v1/auth/login` - 登录
- `GET /api/v1/auth/me` - 获取当前用户

### 修炼相关
- `POST /api/v1/cultivate/start` - 开始修炼
- `POST /api/v1/cultivate/end` - 结束修炼
- `GET /api/v1/cultivate/status` - 获取修炼状态
- `GET /api/v1/cultivate/history` - 获取修炼历史

### 聊天相关
- `POST /api/v1/chat` - 发送消息
- `GET /api/v1/chat/history` - 获取聊天历史
- `DELETE /api/v1/chat/history` - 清空聊天历史

### 每日总结相关
- `POST /api/v1/daily/summary` - 生成每日总结
- `GET /api/v1/daily/summary` - 获取每日总结
- `GET /api/v1/daily/summaries` - 获取总结列表

## 主题色彩

| 颜色 | 色值 | 用途 |
|------|------|------|
| 深夜黑 | `#0a0a0f` | 背景色 |
| 深靛蓝 | `#1a1a2e` | 次要背景 |
| 金色 | `#d4af37` | 主色调/强调 |
| 亮金 | `#f4d03f` | 悬停/高亮 |
| 青色 | `#00d4ff` | 灵气/信息 |

## 开发者

由 OpenClaw 开发助手构建
